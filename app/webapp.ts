// import { ipcRenderer } from "electron";
// import { createNode } from 'dop';

// let serverDop = createNode();
// serverDop.open((msg: any) => ipcRenderer.send(msg));

// ipcRenderer.on('channel', (event, message) => {
//     serverDop.message(message);
// });

